<h1><%= @room.name %></h1>

<ul>
  <% @books.each do |book| %>
    <li>
      <%= link_to book.author.first_name + (book.author.last_name || "") + " - " + book.title, room_book_path(@room, book) %>
    </li>
  <% end %>
</ul>